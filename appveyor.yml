build: 'off'
environment:
  matrix:
    - nodejs_version: '10'
    - nodejs_version: '12'
install:
  - ps: 'Install-Product node $env:nodejs_version x64'
  - npm ci
test_script:
  - node --version
  - npm --version
  - npm test
